export default function StubPage({ title }: { title: string }) {
  return (
    <main className="max-w-2xl mx-auto px-4 pt-5 pb-36">
      <h1 className="text-2xl font-extrabold text-sw-dark">{title}</h1>
    </main>
  )
}
