export function track(_event: string, _props?: Record<string, unknown>) {
  // PostHog / Meta land with the rest of tracking. Wave 2 keeps call sites.
}

export function attributionPayload() {
  return {}
}
